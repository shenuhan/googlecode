package fr.game.engine.fighter;

import java.util.Collection;

import fr.android.api.annotation.Managed;

@Managed
public interface Army {
	Collection<Fighter> getFighters();

	void addFigther(Fighter fighter);
	void removeFigther(Fighter fighter);

	boolean isFighterPresent(Fighter fighter);
}
